# Exchange Web Service with NTLM Authentication

The project logs in to a user-defined Exchange using a user name, password, and domain and checks whether the user has the rights to read calendars and create appointments.
